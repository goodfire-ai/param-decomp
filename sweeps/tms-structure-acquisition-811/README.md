# Task 811 split-favoring structure-acquisition falsifier

C=K=100 random-prefix TMS, gamma held at 1 and frequency/dictionary pressure exactly off. Three seeds each at base and 4x imp pressure. This tests the theory correction that c=0 is only a feasibility warm-start and structure must be acquired with an anti-merge surrogate before final minimality/gamma continuation.

Prediction: compared with the authored gamma anneal + frequency term, gamma=1/frequency=0 should prefer redundant splits over irreversible merges. Base pressure may specialize slowly; 4x tests whether the loss-shape guarantee survives a stronger formative force. Primary reads are identity/taxonomy, PGD, centered-CI rank, and split vs merge counts through 20k.
