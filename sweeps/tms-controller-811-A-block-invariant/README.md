# Task 811 basis-invariant block-GradMax case A

Same 40k scientific config as case A, but one lifecycle transaction may price up to
eight exact-null slots per site with implicit block power iteration. A direction is born
only when its descent derivative is positive on all three independent fresh-referee/data
splits; there is no raw singular-value threshold and no selected k. The cap is a systems
work bound. Whole-batch trials remain function-preserving and transactional.
