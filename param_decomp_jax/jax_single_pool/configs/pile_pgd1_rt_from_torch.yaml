# JAX leg of the torch-vs-jax RUNTIME comparison, single-step-PGD track (n_steps=1,
# step_size=1.0, mask_scope unique_per_datapoint == bsc). Counterpart to the torch legs
# ai-rt-torch-featjax-pgd1 / ai-rt-torch-main-pgd1 (both pd-lm on
# pile_llama_simple_mlp-4L_pgd_ss1.0.yaml). Reuses the existing pgd1 torch-mirror config.
# Launch: pd-jax-lm, 2 nodes (16 GPUs, parity with torch --dp 16).
torch_config: torch/pile_llama_simple_mlp_4l_pgd1.yaml
run_name: ai-rt-jax-pgd1
out_dir: /mnt/data/artifacts/mechanisms/param-decomp/runs
remat_recon_forwards: false
